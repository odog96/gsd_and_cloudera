# Secrets — never commit real tokens
# token.txt is intentionally tracked (placeholder only) — real tokens are protected via skip-worktree
*.token

# Internal session context — not for readers
CLAUDE.md

# Generated output — created by GSD at runtime
attempt-*/
archive/

# GSD internals
.gsd/
.pi/
.bg-shell/

# Python
__pycache__/
*.pyc
*.pyo

# Editor / OS
.DS_Store
*.swp
